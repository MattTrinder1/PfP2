App As appinfo:
    BackEnabled: =false
    OnStart: |
        =Set(
            glbAppVersion,
            1
        );
        Set(
            glbDebug,
            true
        );
        //this value is a placeholder which is updated by the build pipeline, don't edit it manually
        //it's commented it out to avoid it being formatted, which prevents the pipeline from finding it
        //Basically - leave it alone !
        //Set(glbAppVersion,1);
        //Set(glbDebug,false);
        //try and workout if we are running in a browser
        ClearCollect(
            collName,
            true
        );
        // here I created a collection with a single value: true //
        SaveData(
            collName,
            "fileName"
        );
        // try to save that collection wich doesn't work on a browser, just mobile app //
        Clear(collName);
        // delete the value I collected previously //
        LoadData(
            collName,
            "fileName",
            true
        );
        // try to load the data to an emptied collection, if it doesn't load it is because we aren't on mobile //
        If(
            IsEmpty(collName),
            Set(
                glbBrowser,
                true
            )
        );
        // finally, check if the data was loaded corectly and only do the action that i want if so //
        Set(
            gblLoading1,
            false
        );
        Set(
            copsGreen,
            RGBA(
                187,
                221,
                140,
                1
            )
        );
        Set(
            copsRed,
            RGBA(
                245,
                154,
                154,
                1
            )
        );
        Set(
            ypnc,
            0
        );
        Set(
            errormessage,
            false
        );
        Set(
            K,
            0
        );
        Set(
            y1,
            0
        );
        Set(
            addcontactmethod,
            false
        );
        Set(
            contactworkphone,
            false
        );
        Set(
            contactmail,
            false
        );
        Set(
            contacthomephone,
            false
        );
        Set(
            contactmobile,
            false
        );
        Set(
            ycontactmethod,
            0
        );
        Set(
            i,
            0
        );
        Set(
            y,
            0
        );
        LoadData(
            Contactoffline,
            "Contactoffline",
            true
        );
        Reset(txtIncidentSuffix);
        Reset(radReportingOfficer);
        Reset(radSingleOfficerEvent);
        Set(
            Txt_incident_suffixvar,
            ""
        );
        Set(
            incidentnumbervar,
            "CP" & "-" & Text(
                dtpIncidentDate.SelectedDateTime,
                "[$-en-GB]yyyymmdd"
            ) & "-" & txtIncidentSuffix.Text
        );
        LoadData(
            Usersalllocal,
            "Usersalllocal",
            true
        );
        LoadData(
            Userlocal,
            "Userlocal",
            true
        );
        LoadData(
            ContactRoleoffline,
            "ContactRoleoffline",
            true
        );
        LoadData(
            'Sudden death offline',
            "Sudden death offline",
            true
        );
        LoadData(
            contactmethodoffline,
            "contactmethodoffline",
            true
        );
        LoadData(
            TPA,
            "TPA",
            true
        );
        LoadData(
            Housesecureoffline,
            "Housesecureoffline",
            true
        );
        LoadData(
            Housetemperatureoffline,
            "Housetemperatureoffline",
            true
        );
        LoadData(
            Maritaloffline,
            "Maritaloffline",
            true
        );
        LoadData(
            Burialorcremationoffline,
            "Burialorcremationoffline",
            true
        );
        LoadData(
            StaffTitleoffline,
            "StaffTitleoffline",
            true
        );
        LoadData(
            PerceivedEthnicityoffline,
            "PerceivedEthnicityoffline",
            true
        );
        LoadData(
            Genderoffline,
            "Genderoffline",
            true
        );
        LoadData(
            countriesoffline,
            "countriesoffline",
            true
        );
        LoadData(
            UsedContactRoles,
            "UsedContactRoles",
            true
        );
        LoadData(
            'Property offline',
            "Property offline",
            true
        );
        Set(
            phoneMatchString,
            "^[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9]$"
        );

